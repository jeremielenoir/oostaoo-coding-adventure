{
  "routes": [
    {
      "method": "GET",
      "path": "/factures",
      "handler": "Facture.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/factures/count",
      "handler": "Facture.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/factures/:id",
      "handler": "Facture.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/factures",
      "handler": "Facture.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/factures/:id",
      "handler": "Facture.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/factures/:id",
      "handler": "Facture.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
