{
  "routes": [
    {
      "method": "GET",
      "path": "/api/factures",
      "handler": "Facture.find",
      "config": {
        "policies": []
      }
    },

   
    {
      "method": "GET",
      "path": "/api/factures/count",
      "handler": "Facture.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/api/factures/:id",
      "handler": "Facture.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/api/factures",
      "handler": "Facture.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/api/factures/:id",
      "handler": "Facture.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/api/factures/:id",
      "handler": "Facture.destroy",
      "config": {
        "policies": []
      }
    }, {
      "method": "GET",
      "path": "/api/facture-report-pdf/:id",
      "handler": "Facture.generateBillPdf",
      "config": {
        "policies": []
      }
    }
  ]
}
